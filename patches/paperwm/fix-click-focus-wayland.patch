--- a/tiling.js
+++ b/tiling.js
@@ -1941,6 +1941,7 @@
                 let windowAtPoint = !Gestures.gliding && this.getWindowAtPoint(x, y);
                 if (windowAtPoint) {
                     ensureViewport(windowAtPoint, this);
+                    Main.activateWindow(windowAtPoint);
                 }
 
                 spaces.selectedSpace = this;
