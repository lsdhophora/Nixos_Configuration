--- a/desktoppackage/contents/lockscreen/MainBlock.qml
+++ b/desktoppackage/contents/lockscreen/MainBlock.qml
@@ -63,6 +63,14 @@ SessionManagementScreen {
             text: PasswordSync.password
 
             placeholderText: i18ndc("plasma_shell_org.kde.plasma.desktop", "@info:placeholder in text field", "Password")
+            // The lock screen sets the Complementary color set, whose text
+            // colors are near-white (and flip to white after resume) on the
+            // light lock background. Force the View color set instead, so the
+            // echo, the placeholder and the embedded show-password button all
+            // follow the same palette as the field background. Do not pin the
+            // colors: they must stay readable in both light and dark themes.
+            Kirigami.Theme.inherit: false
+            Kirigami.Theme.colorSet: Kirigami.Theme.View
             focus: true
             enabled: !authenticator.graceLocked
 
